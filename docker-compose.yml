version: "3.9"
services:
  db:
    image: mysql:8.0
    container_name: agroai-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: agroai123
      MYSQL_DATABASE: agroai
    ports:
      - "3306:3306"
    volumes:
      - ./mysql-data:/var/lib/mysql
